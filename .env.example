# Copy to .env and fill in. Real environment variables always win over .env.
# Keys are only needed for the web UI's LLM features (web/app.py);
# the core engine, CLI validate/build and tests run without them.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
