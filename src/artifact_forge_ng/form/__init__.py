"""The CAD-free Form IR and everything measured on it — section
profiles, part features (holes, cutboxes, channels, ribs, fields), the
recipe-op registry (``recipe_ops_core.RECIPE_OPS``) and the ``checks_*``
modules that implement form-level validators."""
