schema: features/v1
# Showcase feature vocabulary. The spare/jig feature ids moved to core
# together with their ops (core data/features.yaml); only the education
# wave's vocabulary remains pack-side.
features:
  - id: tolerance_ladder_row
    description: row of test bores of monotonically growing clearance around a printed reference pin
    verified_by:
      - form.ladder_steps_ok
      - topology.bores_open
      - topology.pins_present
