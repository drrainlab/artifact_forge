# Example YAML pack (L0/L1)

Presets and YAML archetypes on the existing op registry — no Python
beyond the two-line `register()`. Copy, rename
(`af_pack_example_yaml` -> your id, entry-point name unique), fill
`data/archetypes/` and `examples/`, keep every example validating.
