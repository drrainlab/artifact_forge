# Example Python pack (L2)

YAML archetypes plus NEW form checks: names declared first (idempotent
`declare()` at import), implementations self-register via
`register_probe`, and every check ships PASS / FAIL / n-a tests on real
op-built forms. New recipe ops (L3) need maintainer discussion first —
see `recipe_ops/README.md`.
